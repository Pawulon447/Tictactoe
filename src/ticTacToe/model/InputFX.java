package ticTacToe.model;

public class InputFX {

    /*
    public static void putSymbol(Button button, Board board, int x, int y,char playerSymbol){

        button.setOnAction(value -> {
            button.setText(String.valueOf(getPlayerSymbol()));
            button.setDisable(true);
            board.fillField(x, y, playerSymbol);
            board.displayBoard();
            board.checkWin(playerSymbol);

            //board.checkDraw();
            playerSymbol = changePlayer();
            if(playerSymbol=='O'){
                botMove();
                playerSymbol = changePlayer();
            }

        });
    }

    public char changePlayer(){
        if(playerSymbol=='X'){
            return 'O';
        }else return 'X';
    }*/


}
